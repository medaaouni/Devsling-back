# Devsling-back
Crud Car Sales Garage application with spring-boot

## Setting Up the Postgresql Database 
The application uses a containerized PostgreSQL as its database

execute the following command in the directory containing docker-compose.yml
```bash
  docker-compose up
```
